const audio=document.querySelector('#backgroundMusic'),musicControl=document.querySelector('#musicControl'),form=document.querySelector('#heartForm'),noteInput=document.querySelector('#loveNote'),characterCount=document.querySelector('#characterCount'),reveal=document.querySelector('#revealScreen'),savedMessage=document.querySelector('#savedMessage'),progress=document.querySelector('#progressBar');
audio.volume=.22;
function fadeAudio(target,duration=550){const start=audio.volume,started=performance.now();function step(now){const value=Math.min((now-started)/duration,1);audio.volume=start+(target-start)*value;if(value<1)requestAnimationFrame(step)}requestAnimationFrame(step)}
async function toggleMusic(){if(audio.paused){try{audio.volume=0;await audio.play();fadeAudio(.22);musicControl.classList.add('playing');musicControl.setAttribute('aria-label','Pause background music')}catch{musicControl.classList.remove('playing')}}else{fadeAudio(0);setTimeout(()=>audio.pause(),560);musicControl.classList.remove('playing');musicControl.setAttribute('aria-label','Play background music')}}
musicControl.addEventListener('click',toggleMusic);noteInput.addEventListener('input',()=>characterCount.textContent=`${noteInput.value.length} / 280`);
form.addEventListener('submit',event=>{event.preventDefault();const message=noteInput.value.trim();if(!message)return;savedMessage.textContent=`“${message}”`;reveal.classList.add('active');reveal.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'});
function closeReveal(){reveal.classList.remove('active');reveal.setAttribute('aria-hidden','true');document.body.style.overflow=''}document.querySelector('#closeReveal').addEventListener('click',closeReveal);document.querySelector('#restart').addEventListener('click',()=>{closeReveal();noteInput.value='';characterCount.textContent='0 / 280';document.querySelector('#home').scrollIntoView({behavior:'smooth'})});window.addEventListener('keydown',event=>{if(event.key==='Escape'&&reveal.classList.contains('active'))closeReveal()});window.addEventListener('scroll',()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${max?(scrollY/max)*100:0}%`},{passive:true});
